# Odoo API Keys (Required for core functionality)
# Get these from Odoo: Settings → Integrations → Access Tokens

# Implementation Odoo (source of Studio customizations)
IMPL_ODOO_URL=https://your-implementation-instance.odoo.com
ODOO_IMPL_API_KEY=your_implementation_api_key_here

# Development Odoo (project management)
DEV_ODOO_URL=https://your-development-instance.odoo.com
ODOO_DEV_API_KEY=your_development_api_key_here

# AI Provider API Keys (Optional - for user story enrichment only)
# Choose ONE provider based on your preference:

# OpenAI (default provider for enrich-stories command)
OPENAI_API_KEY=sk-your_openai_key_here

# Anthropic (alternative provider - use with --provider anthropic flag)
ANTHROPIC_API_KEY=sk-ant-your_anthropic_key_here

# AI Configuration (Optional - set defaults for enrichers)
# Provider: openai or anthropic (default: openai)
AI_PROVIDER=openai

# Model: Specify the AI model to use (defaults by provider if not set)
# OpenAI default: gpt-4
# Anthropic default: claude-sonnet-4-5-20250929
# Examples: gpt-4o, gpt-3.5-turbo, claude-opus-4-5-20251101
# Use: .odoo-sync/cli.py anthropic-models (to list all available Claude models)
AI_MODEL=gpt-4

# Notes:
# - Core workflow (extract, generate-modules, generate-todo, sync) only needs Odoo keys
# - User story enrichment (enrich-stories, enrich-all) requires an AI API key
# - Effort estimation (estimate-effort) does not require any AI API key
# - AI_PROVIDER and AI_MODEL set defaults; override with --provider and --model flags
